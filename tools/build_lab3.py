"""Отчёт по лабораторной работе № 3."""

from report_builder import Report, ROOT, screens

REPO = "https://github.com/Sliva-PanDa/RPiPiP-TripWardrobe"


def build() -> None:
    report = Report(
        number=3,
        topic_lines=[
            "«Механизмы сериализации, локального и долговременного",
            "хранения данных мобильного приложения для планирования",
            "цифрового гардероба в iOS и iPadOS»",
        ],
    )

    report.body(
        "освоить методы сохранения состояния приложения и управления локальными "
        "данными. Научиться работать с легковесными хранилищами, объектными базами "
        "данных и фреймворками управления данными. Изучить механизмы сериализации "
        "и преобразования данных из внешних структурированных форматов.",
        bold_prefix="Цель работы: ",
    )
    report.empty()
    report.centered("Ход работы")
    report.empty()

    report.body("", bold_prefix="Задание.")
    report.body(
        "Реализовать часть проекта, помеченную жёлтым цветом, в соответствии "
        "со своим вариантом. Вариант 15 – мобильное приложение для планирования "
        "цифрового гардероба и сборки чемоданов в поездки с функцией расчёта веса "
        "багажа. К лабораторной работе № 3 относится раздел «Хранение данных»:"
    )
    report.numbered([
        "Основная база данных (реляционная): изначально пустая, наполняется "
        "и обновляется начальными данными при каждом старте приложения; данные, "
        "добавленные пользователем в предыдущих сессиях, не теряются. "
        "Взаимодействие с базой осуществляется фреймворком SwiftData.",
        "Вспомогательное хранилище (UserDefaults): хранит лимит веса багажа "
        "по умолчанию, выбранную тему оформления и маркер первого запуска; "
        "настройки изменяются в отдельном меню внутри приложения.",
    ])
    report.empty()

    report.body(
        f"Состояние проекта на момент сдачи лабораторной работы № 3 зафиксировано "
        f"веткой lab3 репозитория {REPO}."
    )
    report.body(
        "Схема базы данных описана пятью классами, отмеченными макросом @Model: "
        "SeasonEntity (сезон или стиль), LookEntity (образ), ItemEntity (вещь), "
        "TripEntity (поездка) и PackedItemEntity (вещь, уложенная в чемодан). "
        "Идентификаторы помечены атрибутом @Attribute(.unique), поэтому повторная "
        "запись той же сущности обновляет строку, а не создаёт дубликат. "
        "Перечисления ItemStatus, TripKind, PackSource и RecordOrigin хранятся "
        "строковыми значениями: SwiftData работает со скалярными типами, а доступ "
        "к ним в коде остаётся типизированным через вычисляемые свойства."
    )
    report.body(
        "Связи между сущностями описаны атрибутом @Relationship. Сезон содержит "
        "образы, образ – вещи, поездка – уложенные вещи; во всех трёх случаях "
        "задано правило deleteRule: .cascade и указана обратная связь параметром "
        "inverse. Поэтому удаление образа автоматически удаляет входящие в него "
        "вещи, а удаление поездки – все позиции её чемодана; целостность базы "
        "поддерживается фреймворком, а не прикладным кодом."
    )
    report.body(
        "Контейнер базы ModelContainer создаётся в точке входа приложения: он "
        "описывает схему Schema и конфигурацию хранилища. Основной контекст "
        "mainContext передаётся репозиторию SwiftDataRepository, который выполняет "
        "выборки FetchDescriptor, вставку, изменение и удаление записей и вызывает "
        "save(). Если существующий файл хранилища окажется несовместим со схемой, "
        "приложение поднимается на временной базе в памяти и остаётся "
        "работоспособным."
    )
    report.body(
        "Репозиторий реализует те же протоколы WardrobeProviding и TripStoring, "
        "что и хранилище в памяти из лабораторной работы № 2, поэтому экраны "
        "и модели представления при переходе на базу данных не изменились: в точке "
        "входа приложения в контейнер зависимостей подставлен другой объект. "
        "Сущности базы наружу не выдаются – репозиторий отображает их в структуры "
        "предметной области, а закэшированные снимки seasons и trips помечены "
        "как наблюдаемые, поэтому SwiftUI перерисовывает экраны после каждого "
        "изменения базы."
    )
    report.body(
        "Начальные данные описаны каталогом – структурированным документом JSON. "
        "Разбор выполняется протоколом Decodable: структуры CatalogDTO, SeasonDTO, "
        "LookDTO и ItemDTO объявляют перечисление CodingKeys, потому что имена "
        "ключей в документе заданы в стиле snake_case (season_id, weight_grams, "
        "updated_at) и не совпадают с именами свойств. Дата обновления каталога "
        "разбирается стратегией dateDecodingStrategy = .iso8601."
    )
    report.body(
        "Слияние каталога с базой выполняет CatalogSyncService. Записи "
        "сопоставляются по полю remoteID: существующие обновляются справочными "
        "полями (название, категория, вес, значок), отсутствующие добавляются, "
        "а записи серверного происхождения, исчезнувшие из каталога, удаляются. "
        "Пользовательские поля при обновлении сохраняются: статус вещи «В шкафу», "
        "«В поездке» или «В стирке» остаётся тем, который выставил пользователь, "
        "начальный статус из каталога применяется только при первом добавлении "
        "записи. Поездки и записи, созданные пользователем (origin == .local), "
        "синхронизация не затрагивает вовсе. Метод возвращает отчёт SyncReport "
        "с числом добавленных, обновлённых, удалённых и сохранённых записей, "
        "который выводится на экране настроек."
    )
    report.body(
        "Вспомогательное хранилище UserDefaults инкапсулировано классом AppSettings. "
        "В нём хранятся три значения: лимит веса багажа по умолчанию "
        "(settings.defaultLimitGrams), тема оформления (settings.appearance) "
        "и маркер первого запуска (settings.hasCompletedFirstLaunch). Ключи вынесены "
        "в отдельное перечисление, запись выполняется в наблюдателе didSet, поэтому "
        "изменение свойства сразу попадает в хранилище. UserDefaults использован "
        "по назначению – для небольших скалярных настроек; каталог гардероба "
        "и поездки хранятся в базе данных."
    )
    report.body(
        "Маркер первого запуска управляет наполнением демонстрационными данными: "
        "при самом первом старте после установки создаются три поездки, при "
        "последующих запусках этого уже не происходит, и пользовательские чемоданы "
        "остаются нетронутыми. Тема оформления применяется ко всему приложению "
        "модификатором preferredColorScheme, а лимит по умолчанию подставляется "
        "в форму создания новой поездки. Экран настроек показан на рисунке 1."
    )
    report.figure(
        screens("lab3", "iphone-13-settings", "iphone-14-settings-storage"),
        "Рисунок 1 – Настройки: а – тема оформления и лимит багажа; "
        "б – состояние базы данных и отчёт синхронизации",
    )

    report.body(
        "Смена темы оформления вступает в силу немедленно и сохраняется между "
        "запусками приложения, поскольку значение записано в UserDefaults. "
        "Результат переключения на тёмную тему показан на рисунке 2."
    )
    report.figure(
        screens("lab3", "iphone-15-appearance-dark", "iphone-16-wardrobe-dark"),
        "Рисунок 2 – Тёмная тема: а – экран настроек; б – главный экран гардероба",
    )

    report.body(
        "Работа приложения с базой данных на iPad приведена на рисунке 3: данные "
        "те же самые, поскольку и гардероб, и поездки читаются из общего хранилища."
    )
    report.figure(
        screens("lab3", "ipad-13-settings", "ipad-16-wardrobe-dark"),
        "Рисунок 3 – Приложение на iPad: а – экран настроек; б – тёмная тема",
        tablet=True,
    )

    report.body(
        "Слой хранения проверяется модульными тестами на базе данных, созданной "
        "в оперативной памяти (ModelConfiguration с isStoredInMemoryOnly). "
        "StorageTests проверяют разбор каталога с несовпадающими именами ключей, "
        "чтение каталога из ресурсов приложения, наполнение пустой базы, "
        "отсутствие дубликатов при повторной синхронизации, сохранность "
        "выставленного пользователем статуса и созданных поездок после обновления "
        "каталога, обновление справочных полей, удаление исчезнувших из каталога "
        "записей, каскадное удаление вещей вместе с образом и позиций чемодана "
        "вместе с поездкой, а также однократное создание демонстрационных данных. "
        "AppSettingsTests проверяют значения по умолчанию, запись в UserDefaults "
        "и чтение их новым экземпляром, а также сброс настроек."
    )
    report.body(
        "Пример кода описания сущностей базы данных, работы с хранилищами "
        "и сериализации приведён в приложении А."
    )
    report.empty()
    report.body(
        "в ходе выполнения лабораторной работы освоены механизмы локального "
        "и долговременного хранения данных в iOS и iPadOS. Основная база данных "
        "приложения описана средствами SwiftData: пять сущностей со связями "
        "«один ко многим» и каскадным удалением, уникальные идентификаторы, "
        "контейнер ModelContainer и контекст ModelContext. Реализовано наполнение "
        "изначально пустой базы начальными данными при каждом старте приложения "
        "с сохранением пользовательских данных предыдущих сессий. Изучена "
        "сериализация: каталог разбирается протоколом Decodable, несовпадающие "
        "имена ключей JSON сопоставлены со свойствами структур перечислением "
        "CodingKeys. Вспомогательные настройки – лимит веса багажа по умолчанию, "
        "тема оформления и маркер первого запуска – вынесены в UserDefaults "
        "и изменяются на отдельном экране приложения. Замена хранилища в памяти "
        "на базу данных не потребовала правок интерфейса и моделей представления, "
        "что подтвердило выбранное в предыдущей работе разделение слоёв.",
        bold_prefix="Вывод: ",
    )

    report.page_break()
    report.appendix([
        "TripWardrobe/Storage/Entities.swift",
        "TripWardrobe/Storage/CatalogDTO.swift",
        "TripWardrobe/Storage/CatalogSyncService.swift",
        "TripWardrobe/Storage/SwiftDataRepository.swift",
        "TripWardrobe/Storage/AppSettings.swift",
        "TripWardrobe/TripWardrobeApp.swift",
        "TripWardrobe/Views/SettingsView.swift",
    ], ref="lab3")

    report.save(ROOT / "lab3" / "Отчёт ЛР3.docx")


if __name__ == "__main__":
    build()
